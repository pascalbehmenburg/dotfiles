# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                    Windowrules Configuration                ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

# Windows Rules https://wiki.hypr.land/Configuring/Window-Rules/ #
# Updated for Hyprland 0.53+ with new syntax

# Float Necessary Windows
windowrule = match:title rofi, float on
windowrule = match:class ^(org.pulseaudio.pavucontrol), float on
windowrule = match:class ^()$ match:title ^(Picture in picture)$, float on
windowrule = match:class ^()$ match:title ^(Save File)$, float on
windowrule = match:class ^()$ match:title ^(Open File)$, float on
windowrule = match:class ^(LibreWolf)$ match:title ^(Picture-in-Picture)$, float on
windowrule = match:class ^(blueman-manager)$, float on
windowrule = match:class ^(xdg-desktop-portal-gtk|xdg-desktop-portal-kde|xdg-desktop-portal-hyprland)(.*)$, float on
windowrule = match:class ^(polkit-gnome-authentication-agent-1|hyprpolkitagent|org.org.kde.polkit-kde-authentication-agent-1)(.*)$, float on
windowrule = match:class ^(CachyOSHello)$, float on
windowrule = match:class ^(zenity)$, float on
windowrule = match:class ^()$ match:title ^(Steam - Self Updater)$, float on

# Increase the opacity
windowrule = match:class ^(thunar|nemo)$, opacity 0.92
windowrule = match:class ^(discord|armcord|webcord)$, opacity 0.96

# General window rules
windowrule = match:title ^(Picture-in-Picture)$, float on
windowrule = match:title ^(Picture-in-Picture)$, size 550 350
windowrule = match:title ^(Picture-in-Picture)$, move 25%-
windowrule = match:title ^(imv|mpv|danmufloat|termfloat|nemo|ncmpcpp)$, float on
windowrule = match:title ^(imv|mpv|danmufloat|termfloat|nemo|ncmpcpp)$, move 25%-
windowrule = match:title ^(imv|mpv|danmufloat|termfloat|nemo|ncmpcpp)$, size 960 540
windowrule = match:title ^(danmufloat)$, pin on
windowrule = match:title ^(danmufloat|termfloat)$, rounding 5
windowrule = match:class ^(org.mozilla.firefox)$, no_blur on

# Decorations related to floating windows on workspaces 1 to 10
windowrule = match:float true match:workspace w[fv1-10], border_size 1
windowrule = match:float true match:workspace w[fv1-10], border_color $cachylblue
windowrule = match:float true match:workspace w[fv1-10], rounding 8

# Decorations related to tiling windows on workspaces 1 to 10
windowrule = match:float false match:workspace f[1-10], border_size 1
windowrule = match:float false match:workspace f[1-10], rounding 0

# workspace = w[tv1-10], gapsout:0, gapsin:0
workspace = f[1], gapsout:0, gapsin:0

workspace = 1, monitor: DP-2, default:true, persistent:true
workspace = 2, monitor: DP-2, persistent:true
workspace = 3, monitor: DP-2, persistent:true
workspace = 4, monitor: DP-2, persistent:true
workspace = 5, monitor: DP-2, persistent:true
workspace = 8, monitor:eDP-1, persistent:true
workspace = 9, monitor:eDP-1, default:true, persistent:true

# Workspaces Rules End #
# Layers Rules #
layerrule = match:namespace logout_dialog, animation slide top
layerrule = match:namespace wallpaper, animation fade
# Layers Rules End #
